{
  "name": "send-teams-test-notification",
  "version": "1.0.0",
  "private": true,
  "description": "Azure Pipelines task: Teams notification for test results",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "test": "node --import tsx --test tests/**/*.test.ts"
  },
  "dependencies": {
    "azure-pipelines-task-lib": "^5.2.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
